## 工具下载与编译

平台：ubuntu，x86_64，有图形界面

首先下载代码：

```bash
git clone https://github.com/sjtuzc954/QtScrcpy-AutoRecord
```

然后，下载qt5工具链：

```bash
sudo apt install -y qtcreator qtbase5-dev qt5-qmake cmake
```

编译收集工具：

```bash
cd QtScrcpy-AutoRecord
./ci/linux/build_for_linux.sh "Release"
```

可执行程序位于`output/x64/Release`目录下。

## 工具使用注意事项

以下注意事项可结合示例视频理解。

1. 若找不到设备，`QtScrcpy/QtScrcpyCore/src/third_party/adb/linux/adb`可能没有可执行权限，使用`chmod +x`赋予可执行权限；若还是没有，可以勾选”自动刷新“等待一段时间。
2. 双击连接设备前，将屏幕分辨率设置为“原始”，并选择存储的根路径（见示例视频）。每条轨迹会存储在`<根路径>/<品牌名>/<轨迹ID>`下，路径中的品牌名可根据实际情况自行重命名。
3. 滑动操作不要使用鼠标滚轮，使用鼠标拖拽。
4. 需要输入文字时，在“输入”一栏内输入要输入的内容，再按回车，不要直接在屏幕控制窗口中输入。
5. 操作速度不应过快，最好在屏幕UI稳定显示后，再执行下一个动作。
6. 重新启动工具时轨迹ID重置为1，需要手动设置轨迹ID，从上一次收集的轨迹ID之后开始，否则新数据会覆盖旧数据。
7. 关闭窗口后，可执行程序不会退出，可以Ctrl+C退出。
8. 每次截屏，截取的是每个动作发生前的瞬间的屏幕。
9. 如果不通过屏幕控制窗口操控手机，而是直接在手机上操作，动作不会被记录，也不会发生截屏，可以用于处理一些意料之外的事件，或者重置任务执行的初始状态（因为大部分APP会保留上次的搜索结果）。
10. 任务开始时点开始录制，结束并准备录制下一个任务前，需要点结束录制，否则不会保存日志。

## 执行任务注意事项

1. 搜索酒店前先指定日期，因为部分APP在完成酒店输入后，会直接进入搜索列表，之后指定日期比较麻烦。
2. 进入日期选择框后，点”开始原子能力“；退出日期选择框（大部分APP在选择离店日期后，会自动退出）回到酒店界面后，点”结束原子能力“。
3. 日期选择原子能力中，允许有通过滑动寻找目标日期的操作。
4. 对于弹窗广告，可以将关闭广告的动作一起录进轨迹，也可以通过直接操作手机关闭后再继续录制。
5. 每次任务开始时，从APP的首页开始执行任务。
6. APP会记忆上次的搜索结果，搜索新酒店时需要一部分把点”x“也录进去，另一部分先在录制停止时先手动清空再开始从头录制任务。

## 主要收集的几种任务

APP：携程、同城、飞猪、去哪儿旅行、华住会。

任务分类：

在当前城市完成的：
1. “查询/搜索xxx酒店价格”，直接输入酒店名/酒店品牌
2. ”查询/搜索xxx附近的/周围的/旁边的酒店价格“，在酒店名的同一个输入栏输入位置（大部分APP的这一栏都是”位置/品牌/酒店“，既可以输入位置又可以输入品牌），可以是著名地标、商圈等。这里的地标应该属于默认城市（即一进入酒店页面，不需要改城市，即可搜索到的地标）

需要选择城市完成的：

3. 在1的基础上加入城市，通过酒店查询中的”城市选择“功能完成。”城市选择“界面中，可以进行搜索，也可以直接找到并点击目标城市，两种最好均匀分布。
4. 在2的基础上加入城市，即指定的地标不属于默认城市，需要修改城市后，再输入地标，注意地标和所属城市的对应关系。

需要指定日期的：

5. 在1-4的基础上，加入确切日期”x月x日到x月x日“（注意，录制时先选择日期，见执行任务注意事项）
6. 在1-4的基础上，加入模糊日期，例如“明天””后天“”下周“等等（录制时仍然选择确切日期）

示例任务：

1. 查询汉庭酒店价格
2. （当前城市为上海）查询外滩附近的酒店价格
3. 查询北京的汉庭酒店价格
4. （当前城市为上海）查询北京的故宫博物院周围的酒店价格
5. 查询汉庭酒店价格，入住时间为5月10日到5月15日
6. 查询北京的故宫博物院旁边的酒店的价格，入住时间为明天到后天

具体的任务列表在”任务列表“文档中给出。
